# 0803 작업 리포트 (아침에 읽어주세요)

요청 두 가지를 했습니다.

1. **프로그램을 홈페이지에 올리는 공식 시스템** — 만들었고 검증까지 끝냈습니다
2. **학교 예산표 자동 매칭** — 데이터 구조 조사를 마쳤습니다 (결론: 됩니다)

**기존 사이트 파일은 하나도 건드리지 않았습니다.** `git status` 에 `_system/` 폴더 추가만 뜹니다.

---

## 1. 프로그램 업로드 시스템

### 지금까지 vs 앞으로

| | 지금까지 | 앞으로 |
|---|---|---|
| 상세 페이지 | HTML 20~40KB 손코딩 | 시트 한 줄 |
| 목록 카드 | `together/index.html` 직접 수정 | 자동 |
| sitemap | 직접 수정 | 자동 |
| 검색·공유 태그 | 직접 수정 | 자동 |
| 디자인 변경 | 페이지 9개 각각 수정 | 템플릿 1개 수정 |

작업 흐름:

```
구글 시트에 한 줄  →  node _system/sync.js  →  node _system/build.js
     →  미리보기 확인  →  node _system/build.js --apply  →  git push
```

사용법은 **`_system/README.md`** 에 정리해뒀습니다.

### 알게 된 것 — 페이지가 이미 데이터 구조였습니다

각 프로그램 페이지 안에 `programData` 라는 객체가 이미 있었습니다.
나머지(CSS·렌더 함수·푸터)는 전부 복붙이었고요. 그래서 **새로 설계한 게 아니라, 이미 있던 구조를 페이지 밖으로 꺼낸 것**입니다. 그만큼 위험이 적습니다.

### 검증 결과

말로 "된다" 가 아니라 실제로 대조했습니다. `node _system/verify.js` 로 언제든 다시 돌릴 수 있습니다.

**검증 1 — 내용이 새지 않는가** (원본 → 시트 → 다시 데이터)

```
9개 중 5개 완전 일치 · 내용 손실 0건
```

- 완전 일치 5개: domino, schoolping, facilitation, violence-prevention, touching-voice
- leadership 6건은 **손실이 아니라 표준화**입니다 (정보 칩 순서를 대상·시간·인원·장소로 통일 등)
- 구형 3개(talkshow, job-lab, ceo-talk)는 구조가 달라 **시트에서 꺼둔 상태** — 기존 페이지 그대로 유지됩니다

**검증 2 — 디자인이 깨지지 않는가**

```
동일  violence-prevention
동등  schoolping    (렌더 로직 동일, 변수명·들여쓰기만 다름)
동등  facilitation  (동일)
```

**검증 3 — 목록 카드** (`node _system/verify-cards.js`)

```
카드 9장 전부 원본과 일치
```

### 검증하다가 발견한 것 3가지

**① 검색엔진이 보는 내용과 사용자가 보는 내용이 달랐습니다**

각 페이지 `<main>` 안에 자바스크립트 없이도 보이는 초기 텍스트가 손으로 쓰여 있는데,
실제 데이터와 어긋나 있었습니다. 예를 들어 `violence-prevention` 은

- 크롤러가 보는 제목: `전교생 관계형성 레크리에이션`
- 사용자가 보는 제목: `단합 레크리에이션`

새 시스템은 이 둘을 항상 자동으로 맞춥니다.

**② 카카오톡·페이스북 공유 미리보기가 비어 있었습니다**

`og:image`, `og:title`, `og:description` 이 **9개 페이지 전부 없었습니다.**
프로그램 링크를 보내면 미리보기 이미지가 안 뜨는 상태였습니다. 영업에서 링크를 자주 보내신다면 체감될 부분입니다.
템플릿에 넣어서 이제 **전 페이지 4/4** 로 채워집니다.

**③ 만들어두고 안 쓰는 섹션이 있습니다**

`renderStructures` (활동 유형 예시) 함수가 정의돼 있고 데이터도 있는데,
`renderPage` 에서 호출을 안 해서 **화면에 안 나옵니다.** schoolping·facilitation·violence-prevention 3개 해당.
살릴지 뺄지 알려주시면 반영하겠습니다. (지금은 원본 그대로 뒀습니다)

### 아침에 하실 것

1. `_system/README.md` 읽기 (5분)
2. 구글 시트 만들고 `_system/data/sheet/` 의 CSV 5개를 탭 5개에 붙여넣기
3. 시트 **파일 > 공유 > 웹에 게시** → 주소 5개를 `_system/sheet.config.json` 에 넣기
4. `node _system/build.js` 로 미리보기 만들고 `_system/_build/` 를 브라우저로 열어 눈으로 확인
5. 괜찮으면 `node _system/build.js --apply`

> ⚠️ `--apply` 를 붙이기 전에는 실제 사이트가 절대 안 바뀝니다. 편하게 확인하셔도 됩니다.

### 확인이 필요한 판단 2가지

- **leadership·domino·touching-voice** 는 CSS 값이 표준과 조금씩 다른 변종입니다.
  새 시스템으로 다시 만들면 표준 디자인으로 통일됩니다. 미리보기로 먼저 보시고 결정하세요.
- **구형 3개** 를 표준으로 옮기려면 `keywords` / `studentExperiences` / `process` 를
  `활용장면` + `진행흐름` 으로 다시 써야 합니다. 원하시면 제가 옮겨드리겠습니다.

---

## 2. 학교 예산표 매칭 — 조사 결과

전체 내용은 **`_system/학교예산_조사.md`** 에 있습니다. 요약하면:

**됩니다. 단 학교 홈페이지를 훑는 방식이 아니라 국가 공시 데이터를 쓰는 방식으로요.**

- 학교 홈페이지 크롤링은 비현실적입니다 (1만 곳 넘고, CMS 제각각, 예산서가 .hwp 첨부)
- 대신 모든 학교가 법에 따라 같은 형식으로 신고하는 **학교알리미 공시 데이터**가 있습니다
- **학교알리미 공개용데이터는 로그인 없이 무료 다운로드** 가능합니다 (재정/시설/설비 탭)

**핵심은 예산이 우리 프로그램과 맞물리는 수준까지 쪼개져 있다는 점입니다.**

```
정책사업 → 단위사업 → 세부사업 → 세부항목
              └ 창의적 체험활동
                   ├ 자율·자치활동   ← 레크리에이션 · 스쿨핑 · 학폭예방
                   ├ 동아리활동
                   └ 진로활동        ← 터칭보이스 · 미래직업연구소 · 진로톡
```

**아직 확인 못 한 것**: 공시 파일이 세부사업 수준까지 담는지, 아니면 총액만인지.
→ **표본 파일 1개만 받아보면 바로 확정됩니다.** 로그인이 필요 없어 언제든 가능합니다.

**다음 단계**: 학교알리미 → 공개용데이터 → 재정/시설/설비 → 교육청 1곳·최근 연도 1개 다운로드
→ 파일 주시면 컬럼 확인하고 프로그램별 "예산이 잡혀 있는 학교 목록" 만드는 작업으로 이어가겠습니다.

---

## 만든 파일

전부 `_system/` 안에 있습니다. 기존 사이트 파일은 그대로입니다.

| 파일 | 하는 일 |
|---|---|
| `README.md` | **사용법 — 여기부터 읽으세요** |
| `학교예산_조사.md` | 학교 예산 데이터 조사 전문 |
| `sync.js` | 구글 시트 → CSV |
| `build.js` | CSV → 페이지·목록·sitemap |
| `schema.js` | 시트 컬럼 정의 (컬럼 바꾸려면 여기) |
| `template.html` | 페이지 껍데기 (디자인 바꾸려면 여기) |
| `verify.js` `verify-cards.js` | 검증 |
| `data/sheet/*.csv` | **시트에 붙여넣을 CSV 5개** |
| `data/programs.json` | 기존 페이지에서 뽑은 원본 데이터 |
| `_build/` | 미리보기 결과 |
| `extract*.js` `to-sheet.js` `*-compare.js` `make-template.js` | 최초 1회용 도구 (이미 실행됨) |
